# $Id$

Bugs:
- dialup: don't always start smpppd and KInternet (#26382)
- dialup: don't run KInternet in ncurses (#27891)
- dialup: run KInternet according to the number of connections (#29475)
- lan: UNIQUE missing after nfs install (#25739)
- provider: provider & `immediate (#25432)
- provider: provider0 (#17564)
- provider: wrong providers handling (installation: conflicting types) (#31508)
- all: required packages not installed during installation (#31536)
- isdn: cleanup [ISDN]

Improvements:
- dns: two resolv.conf scrconfs
- lan: check IP presence (#9951)
- lan: check link presence (see sysconfig.rpm how is it done)
- lan: myrinet: check for driver presence [warning + info]
- lan: remove writing of NETWORK (also BROADCAST?), probably not used
- modem: check existence of the modem device in ModemDialog
- routing: restart only routing
- routing: check already present default route (partially #11132)
- routines: check for yast2-mail presence
- routines: support plip, slip, myri, ppp (serial cable, not modem)
- routines: better kinternet handling (kdelibs, kdebase?)
- dialup: start and setup smpppd only if >0 devs
- dialup: info popup (configured + kinternet, cinternet)
- dialup: run|stop kinternet, if DISPLAY and (not)running (#14320)
- provider: offer to delete provider after device delete if not used (#16094)
- provider: warn if deleting default provider for modem|isdn|dsl (#17497)
- provider: delete default route if removed (#10739)
- lan: warn before the network restart (#27137, #19851) [WARN]
- dns: better "DNS and Hostname" and "Hosts" titles (#29722)
- lan: more cards of the same type (see also #31441) (#31535)
- lan: netcards without a driver (what to do on s390?, warn?) (#29296)
- lan: builtin IBM cards? (#19910)
- dns: better message for broken fields (eg. domain) (#22802)
- lan: s390 devices numbering (#23332)
- lan: detect wlan cards (#23491)
- lan: yp configuration failed (#25506)
- lan: ssh installation (install.inf format) (#24836)
- all: don't always ask for the mail config (#31472)

Enhancements:
- proxy: use immediately (#23728)
- detection (netprobe, dhcpcd) #15489 (see also #7287, #13200, #9951)
- lan: defaut gw in the first dialog (#13450)
- modem: winmodems support (#25220, #15556)
- lan: dual port network cards #10077, #12387 [see 2002-05.txt]
- lan: compiled in kernel drivers
- modem: speaker volume as combo (or better as slider)
- modem: pap|chap secrets writing
- providers: no-global-values
- providers: preload the country providers (other upon request)
- providers: -> HWDB-NG
- all: edit without restart (reload)
- device: use map instead of variables (-> fill all variables)
- package: include all agents (ag_xauth, ag_resolver, ag_hostnames, ...)
- package: require yast2-language, yast2-mail, ...
- hardware: cable connection (plip, slip, usb)
- hardware: wireless connection (irda, bluetooth)
- provider: use INS file (#25774)
- feature: IP-masquerading (#16131), is already in the firewall
- routing: default route without GW (#23977)
- lan: use ethtool to map drivers to device names (#17346)
- host: scan for hosts on the local network [Add]
- host: add (update) all hosts in the local network
- host: show system hosts checkbox (like users have)
- routing: other fields (see routes(5))
- routing: possibly rename to Route
- lan: temporary interface disable (#24926)
- all: temporary interface disable
- dsl: flat rate (#27840)
- ifcfg-usb-usb (#22739)
- better network UI [sigi] (#29721)
- support custom provider from INS file (#27955)
- improve modules.conf options merging (maybe obsolete with 2.6) (#29995)
- make DHCP options iface-local (questionable) (#30476)
- propose "http://" for the proxy strings (#31269)
- IP address assigning (#25491)
- edit dsl HW goes through provider again (default provider?) (#24000)
- renumber (rename) ifaces when deleted (#24882)
- wlan|pcmcia switch (#24928)
- allow to change the netcard type (for a broken detection) (#30890)
- skip detect check box (general yast2 feature) (#31478)
- installation: better network configuration (inst_netcfg, create_interfaces)
- dns: update /etc/hosts (#22825)
- dsl: better proposal (#23662)
- dsl: detect hardware (#24462)
- dsl: empty ifcfg-ethX (#17631)
- dummy: dummy device adding (#15960) [suse-network] [DUMMY]
- alias: virtual interfaces (#6627) [ALIASES]
- dns: replace SuSEconfig.hostname [HOSTNAME]
- dialup: mail question only if new device was added
- lan: install dhcpcd|sysconfig|... (before Write)
- lan: install wireless-tools, hotplug, ... if needed (before Write)
- IPv6 support (tunnels)
- wireless improvements (waiting for jg@suse.de)

Documentation:
- example dhcpcd-eth0.info
- example intstall.inf's
- Net-HOWTO link
- s390 devices (see bellow, also from mails)
- overall device overview (see bellow, also required data like IP, RI, NM)
- UIMODE: link to autodocs
- doc: adding network device mini-howto: grep for "escon" for example
- doc: include public_html/network, dia, ...

##############################################################################

ISDN:
- code cleanup (use yast2-network structure: Provider, devices, ...)
- UI cleanup (http://w3.suse.de/~sigi/yast2/modules/isdn/81isdn/isdn81_improvement_it01.html)
- isdn: start mail after config (#18256)

HOSTNAME:
- replace SuSEconfig.hostname (#22825)
- /var/adm/SuSEconfig/oldhostentry:
notebook.suse.cz
1.2.3.4
- add "127.0.0.1 localhost" if missing
- grep [IPADDRs] frrom sysconfig/network/ifcfg-*
- read HOSTNAME from /etc/HOSTNAME
- for all [IPADDRs] if IPADDR <> 127.0.0.1 || 0.0.0.0:
- add "$IPADDR $HOSTNAME $NICKNAME" for the first item
- add "$IPADDR $HOSTNAME" for other IPADDRs
- if [IPADDRs] == [] add "127.0.0.2 $HOSTNAME $NICKNAME"

- remove BEAUTIFY_ETC_HOSTS [comments at top, sort; implement in yast2?]
- remove CHECK_ETC_HOSTS

UpdateHosts("hostname", [IPADDRs]);

- see Host::Update()

WARN:
- #19851, #27137
- warn before rcnetwork restart -> RestartNetwork() | StopNetwork();
  DISPLAY=
  :0.0		OK
  localhost:0.0	OK
  beholder:0.0	OK
  wyvern:11.0	Warn

  SSH_CLIENT	Warn

##############################################################################

TO-SORT:

- all: use rcnetwork reload (but probably only in some cases, things like
  remove 2 eth cards and add them in a different order won't work due to
  the kernel modules not being unloaded)

- routing: summary: split gateway of the other routes
- routing: summary: print other routes in nice form
- routing: summary: print gateway hostname [Gateway: 10.20.0.1 (chimera.suse.cz)]

- dns: summary: print ns and sl in nice form
- dns: summary: print ns hostname [Name server: 10.20.0.1 (chimera.suse.cz)]
- dns: summary: differentiate between 1 and more ns
    [Name servers: 10.20.0.1 (chimera.suse.cz), 10.20.1.16 (yeti.suse.cz)]

- routines: -> FIREWALL=yes|no to each ifcfg-XXX #18840 (support for modems)

##############################################################################

##############################################################################

DOC:
 ESCON = Enteprise System CONnector (something like a mixture of SCSI and
         Ethernet - its a channel, but can carry network traffic)
         - short: its a point-to point network interface

 IUCV  = Inter User Communication Vehicle (means: DMA based network pipe
         between Virtual Machine instances (like vmware) on an s390)
         - short: its a point-to point network interface

DOC:
- doc: dummy: IP/NM/BC
- doc: irlan: IP/NM/BC (eth?)
- doc: irnet: IP/RI (?)
- doc: plip: IP/RI (Parallel Line IP)
- doc: slip: IP/RI (Serial Line IP, obsoleted by PPP)
- doc: ppp: IP/RI (Serial Point-To-Point protocol)

UI proposal:
- detected
- choose your card (current list)
- hw params only (current manual)

Hardware:
- usb: network cabel, ethernet card, TR card, ISDN card, modem
- pcmcia: ethernet card, TR card, ISDN card, modem
- wireless network cards
- fddi card

- make device optional in routes agent
- DefaultDevice(ip)
    check all PPP connections (REMOTEIP)
    other devices
      - take IP
      - if(network(IP) == network(ip)) return x;
- compare with old lan
- where is this used?

Modules:
- see the "*ISDN*" paper

MODULES=$[];	-> Lan.ycp
DELETED=[];	-> Lan.ycp
ReadModules();	-> M
WriteModules();	<- D,M
Select: C,M -> global vars
AddEdit: global vars -> C,M
Delete: global vars -> C,M,D
