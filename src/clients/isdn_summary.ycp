/**
 * File:	clients/isdn_summary.ycp
 * Package:	Configuration of network
 * Summary:	ISDN summary
 * Authors:	Michal Svec <msvec@suse.cz>
 *
 * $Id$
 *
 * Main file for network card configuration.
 * Uses all other files.
 */

{

textdomain "network";

/* The main () */
y2milestone("----------------------------------------");
y2milestone("ISDN summary started");

import "ISDN";
import "Progress";
import "Summary";

Progress::off();
ISDN::Read();
list sum = ISDN::Summary(false);
string ret = sum[0]:"";
Progress::on();

/* Finish */
y2milestone("ISDN summary finished");
y2milestone("----------------------------------------");
return ret;

/* EOF */
}
