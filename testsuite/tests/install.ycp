{

// testedfiles: install.ycp

include "testsuite.ycp";

map READ = $[
    "etc" : $[ "install_inf" : $[ "NetConfig" : "bootp" ]],
];

map InstallInf = $[];
include "network/install.ycp";

// DUMP("ReadInstallInf");
// TEST(``(ReadInstallInf()), [], nil);

InstallInf = $[];
path install_inf = .etc.install_inf;

DUMP("ReadInstallInfItem");
TEST(``(ReadInstallInfItem(install_inf, "bootproto", "NetConfig")), [READ], nil);
DUMP(InstallInf);
TEST(``(ReadInstallInfItem(install_inf, "ipaddr", "IP")), [READ], nil);
DUMP(InstallInf);

}
