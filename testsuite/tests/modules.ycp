{

include "testsuite.ycp";

map READ = $[
    "probe" : $[ "system" : [], ],
    "target" : $[
	"tmpdir" : "/tmp",
    ],
];

TESTSUITE_INIT([READ], nil);
include "network/modules.ycp";

map Module = $[];

DUMP("AddModule");
TEST(``(AddModule("eth")), [], nil);
DUMP(Module);

}
